import { MatPaginatorConfig } from '../../shared/materialconfig/mat-paginator-config';

describe('MatPaginatorConfig', () => {
  it('should create an instance', () => {
    expect(new MatPaginatorConfig()).toBeTruthy();
  });
});
